diff --git a/resources/css/app.css b/resources/css/app.css
index 3e6abea..1d487d4 100644
--- a/resources/css/app.css
+++ b/resources/css/app.css
@@ -9,3 +9,49 @@ @theme {
     --font-sans: 'Instrument Sans', ui-sans-serif, system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji',
         'Segoe UI Symbol', 'Noto Color Emoji';
 }
+
+/*
+ * Tailwind's preflight zeroes out border-width on every element by default,
+ * so utility classes like `border-slate-300` (used across every form in the
+ * app) only set a *color* with no width — the border was never actually
+ * visible. Give every real form control a visible 1px border by default,
+ * plus a clearer focus ring, so inputs read as inputs.
+ */
+input[type='text'],
+input[type='email'],
+input[type='tel'],
+input[type='number'],
+input[type='password'],
+input[type='date'],
+input[type='time'],
+input[type='search'],
+input[type='url'],
+textarea,
+select {
+    border-width: 1px;
+    border-style: solid;
+    border-color: theme('colors.slate.300');
+    transition: border-color 0.15s ease, box-shadow 0.15s ease;
+}
+
+input[type='text']:focus,
+input[type='email']:focus,
+input[type='tel']:focus,
+input[type='number']:focus,
+input[type='password']:focus,
+input[type='date']:focus,
+input[type='time']:focus,
+input[type='search']:focus,
+input[type='url']:focus,
+textarea:focus,
+select:focus {
+    outline: none;
+    border-color: theme('colors.blue.500');
+    box-shadow: 0 0 0 3px rgb(59 130 246 / 0.15);
+}
+
+input[type='checkbox'],
+input[type='radio'] {
+    border-width: 1px;
+    border-color: theme('colors.slate.300');
+}
diff --git a/resources/views/owner/properties/create.blade.php b/resources/views/owner/properties/create.blade.php
index 187727d..5cc325f 100644
--- a/resources/views/owner/properties/create.blade.php
+++ b/resources/views/owner/properties/create.blade.php
@@ -5,6 +5,8 @@
 @section('content')
     <h1 class="text-xl font-semibold text-slate-900 mb-6">Add a new property</h1>
 
+    @include('partials.validation-errors')
+
     <form method="POST" action="{{ route('owner.properties.store') }}" enctype="multipart/form-data"
           class="bg-white border border-slate-200 rounded-xl p-6">
         @csrf
diff --git a/resources/views/owner/properties/edit.blade.php b/resources/views/owner/properties/edit.blade.php
index 08e72b8..52439d8 100644
--- a/resources/views/owner/properties/edit.blade.php
+++ b/resources/views/owner/properties/edit.blade.php
@@ -5,6 +5,8 @@
 @section('content')
     <h1 class="text-xl font-semibold text-slate-900 mb-6">Edit {{ $property->name }}</h1>
 
+    @include('partials.validation-errors')
+
     <form method="POST" action="{{ route('owner.properties.update', $property) }}" enctype="multipart/form-data"
           class="bg-white border border-slate-200 rounded-xl p-6">
         @csrf
diff --git a/resources/views/owner/properties/spaces/_form.blade.php b/resources/views/owner/properties/spaces/_form.blade.php
index bbdd0a0..77e0339 100644
--- a/resources/views/owner/properties/spaces/_form.blade.php
+++ b/resources/views/owner/properties/spaces/_form.blade.php
@@ -82,11 +82,6 @@ class="w-full rounded-lg border-slate-300 focus:ring-blue-500 focus:border-blue-
                 <input type="text" name="attributes[floor]" value="{{ old('attributes.floor', $space->attributes['floor'] ?? '') }}"
                     class="w-full rounded-lg border-slate-300 focus:ring-blue-500 focus:border-blue-500">
             </div>
-            <div>
-                <label class="block text-sm font-medium text-slate-700 mb-1">Unit type</label>
-                <input type="text" name="attributes[unit_type]" value="{{ old('attributes.unit_type', $space->attributes['unit_type'] ?? '') }}"
-                    placeholder="e.g. Studio, 1-Bedroom" class="w-full rounded-lg border-slate-300 focus:ring-blue-500 focus:border-blue-500">
-            </div>
         </div>
 
         <div x-show="propertyType === 'boarding_house'">
diff --git a/resources/views/owner/properties/spaces/create.blade.php b/resources/views/owner/properties/spaces/create.blade.php
index ba80e79..2e1c9b4 100644
--- a/resources/views/owner/properties/spaces/create.blade.php
+++ b/resources/views/owner/properties/spaces/create.blade.php
@@ -7,6 +7,8 @@
 
     <h1 class="text-xl font-semibold text-slate-900 mt-4 mb-6">Add a unit/room to {{ $property->name }}</h1>
 
+    @include('partials.validation-errors')
+
     <form method="POST" action="{{ route('owner.properties.spaces.store', $property) }}" enctype="multipart/form-data"
           class="bg-white border border-slate-200 rounded-xl p-6">
         @csrf
diff --git a/resources/views/owner/properties/spaces/edit.blade.php b/resources/views/owner/properties/spaces/edit.blade.php
index c12f06c..1099a39 100644
--- a/resources/views/owner/properties/spaces/edit.blade.php
+++ b/resources/views/owner/properties/spaces/edit.blade.php
@@ -7,6 +7,8 @@
 
     <h1 class="text-xl font-semibold text-slate-900 mt-4 mb-6">Edit {{ $space->space_number }}</h1>
 
+    @include('partials.validation-errors')
+
     <form method="POST" action="{{ route('owner.properties.spaces.update', [$property, $space]) }}" enctype="multipart/form-data"
           class="bg-white border border-slate-200 rounded-xl p-6">
         @csrf
diff --git a/resources/views/partials/validation-errors.blade.php b/resources/views/partials/validation-errors.blade.php
new file mode 100644
index 0000000..8c1c830
--- /dev/null
+++ b/resources/views/partials/validation-errors.blade.php
@@ -0,0 +1,12 @@
+@if ($errors->any())
+    <div class="mb-6 rounded-xl border border-red-200 bg-red-50 p-4">
+        <p class="text-sm font-medium text-red-800 mb-2">
+            {{ $errors->count() === 1 ? 'There is 1 problem with your submission:' : 'There are '.$errors->count().' problems with your submission:' }}
+        </p>
+        <ul class="list-disc list-inside text-sm text-red-700 space-y-1">
+            @foreach ($errors->all() as $message)
+                <li>{{ $message }}</li>
+            @endforeach
+        </ul>
+    </div>
+@endif
